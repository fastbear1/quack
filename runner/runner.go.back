package runner

import (
	"fmt"

	utils "github.com/fastbear1/quack/internal"
)

func Run(conf *utils.ConfigYaml) {
	// step 1: check database
	dbMeta, err := getDbMeta(conf)
	utils.CheckErrLite(err)
	fmt.Println(dbMeta)
}
